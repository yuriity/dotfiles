# Starship Configuration

To get started configuring starship, create (or open) the following file: ~/.config/starship.toml.

```bash
mkdir -p ~/.config && touch ~/.config/starship.toml
```

Use [documentation](https://starship.rs/config/)
